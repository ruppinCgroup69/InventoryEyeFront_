import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

//recives props (size, title)
export default function header({size,title}) {
  return (
    <View>
      <Text>header</Text>
    </View>
  )
}

const styles = StyleSheet.create({})